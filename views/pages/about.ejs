
    <%- contentFor('hero') %>
    <section class="hero">
        <div class="hero-content">
            <h2>What is this place?</h2>
            <p>This website's purpose is to show any online object I found interesting.</p>

            <a href="/" class="btn btn-lg btn-primary">Home</a>
        </div>
    </section>

    <%- contentFor('body') %>
    <!-- <div class="jumbotron text-center">
        <h1>The brothers</h1>
        <div class="row">
            <% for (user of users) { %>
                <div class="col-sm-3">
                    <h2><%= user.name %>
                    <img src="<%= user.avatar %>" alt="" class="img-responsive">
                </div>
            <% } %>
        </div>
    </div> -->
